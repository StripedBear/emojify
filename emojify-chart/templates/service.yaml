apiVersion: v1
kind: Service
metadata:
  name: emojify
  labels:
    app: emojify
    prometheus: scrappable
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: 8080
  selector:
    app: emojify
